import { StyleSheet } from "react-native";

export const styles = StyleSheet.create ({
    bodyList: {
        margin: 20,
        backgroundColor: '#FFF',
        borderRadius: 20,

    },

    textDescription: {
        margin: 15,
        color: '#000',
        fontSize: 15,
    }
})